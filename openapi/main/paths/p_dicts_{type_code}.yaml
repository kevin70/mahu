parameters:
  - $ref: "../components/parameters/path_type_code.yaml"
get:
  summary: 获取字典类型数据
  operationId: getPublicDict
  security: [ ]
  tags:
    - base
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            $ref: "../components/schemas/PublicDictResponse.yaml"
