type: object
required:
  - object_id
  - upload_url
  - access_url
properties:
  object_id:
    type: integer
    format: int64
    description: 对象 ID
  upload_url:
    type: string
    description: 上传 URL
  access_url:
    type: string
    description: 上传成功后的访问 URL
