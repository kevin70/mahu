post:
  summary: 文件预签名上传
  operationId: createFilePresigned
  tags: [h_asset]
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../components/schemas/AssetCreatePresignedRequest.yaml"
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            $ref: "../components/schemas/AssetCreatePresignedResponse.yaml"
