post:
  summary: 文件预签名上传
  operationId: createFilePresigned
  tags: [h_file]
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../components/schemas/FileCreatePresignedRequest.yaml"
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            $ref: "../components/schemas/FileCreatePresignedResponse.yaml"
