get:
  summary: 获取应用信息
  operationId: getInfo
  tags: [h_help]
  security: []
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            $ref: "../components/schemas/InfoResponse.yaml"
