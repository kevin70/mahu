type: object
required:
  - label
  - type
properties:
  label:
    type: string
    description: 文本
    maxLength: 32
  type:
    type: string
    description: 属性类型
    enum:
      - INPUT
      - SELECT
    x-enum-descriptions:
      - 手动输入
      - 选择
