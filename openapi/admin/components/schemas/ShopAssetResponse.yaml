type: object
required:
  - id
  - created_at
  - uri
properties:
  id:
    type: integer
    format: int64
    description: 主键
  created_at:
    type: string
    format: date-time
    description: 创建时间
  uri:
    type: string
    description: 资源 URI
