type: object
required:
  - id
  - feature_id
  - topic
  - status
  - delay_until
  - attempts
  - max_attempts
  - payload
properties:
  id:
    type: string
    format: uuid
    description: ID
  created_at:
    type: string
    format: date-time
    description: 创建时间
  updated_at:
    type: string
    format: date-time
    description: 修改时间
  feature_id:
    type: integer
    format: int32
    description: 功能 ID（用于追踪统计）
  topic:
    type: string
    description: 主题
  status:
    $ref: "./_status.yaml"
  delay_until:
    type: string
    format: date-time
    description: 下一次可执行时间（含重试）
  attempts:
    type: integer
    format: int32
    description: 已尝试次数
  max_attempts:
    type: integer
    format: int32
    description: 最大重试次数（包括首次执行）
  lock_at:
    type: string
    format: date-time
    description: 任务锁定时间（开始执行时间）
  lease_seconds:
    type: integer
    format: int32
    description: 锁租约(秒)，worker 处理任务允许的最长时间
  payload:
    type: object
    description: 消息内容存储业务所需的所有数据
    additionalProperties: true
  idempotency_key:
    type: string
    description: 幂等键
