parameters:
  - in: path
    name: file_id
    description: 文件流水号
    required: true
    schema:
      type: integer
      format: int64
      minimum: 1
get:
  summary: 定向文件
  operationId: forwardFile
  tags: [h_file]
  responses:
    "302":
      description: OK
      headers:
        Location:
          description: 重写向到证件访问地址
          schema:
            type: string
            format: uri
          example: https://api.example.com/v1/new-resource
