parameters:
  - in: path
    name: code
    required: true
    description: 字典代码
    schema:
      type: string
get:
  summary: 获取字典类型
  operationId: getPublicDict
  security: [ ]
  tags: [ base ]
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            $ref: "../components/schemas/PublicDictResponse.yaml"
