get:
  summary: 字典类型列表
  operationId: listPublicDicts
  tags: [ h-public-dict ]
  security: [ ]
  parameters:
    - name: include_data
      in: query
      description: 是否包涵 `data` 数据
      schema:
        type: boolean
        default: false
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: array
            description: 字典列表
            items:
              $ref: "../components/schemas/PublicDictTypeResponse.yaml"
