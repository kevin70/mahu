get:
  summary: 服务版本
  operationId: gerVersion
  security: [ ]
  tags:
    - base
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            $ref: "../components/schemas/VersionResponse.yaml"
