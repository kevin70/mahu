parameters:
  - in: path
    name: file_id
    description: 文件 ID
    required: true
    schema:
      type: string
      maxLength: 50
get:
  summary: 定向资源
  operationId: forwardFile
  tags: [h_file]
  security: []
  responses:
    "302":
      description: OK
      headers:
        Location:
          description: 重写向到资源访问地址
          schema:
            type: string
            format: uri
          example: https://api.example.com/v1/new-resource
