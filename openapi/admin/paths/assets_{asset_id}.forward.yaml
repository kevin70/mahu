parameters:
  - in: path
    name: asset_id
    description: 资源流水号
    required: true
    schema:
      type: integer
      format: int64
      minimum: 1
get:
  summary: 定向资源
  operationId: forwardAsset
  tags: [h_asset]
  responses:
    "302":
      description: OK
      headers:
        Location:
          description: 重写向到证件访问地址
          schema:
            type: string
            format: uri
          example: https://api.example.com/v1/new-resource
