get:
  summary: 查询系统所有的权限代码
  operationId: allPermits
  tags:
    - system
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: array
            description: 权限代码列表
            items:
              $ref: "../components/schemas/PermitResponse.yaml"
