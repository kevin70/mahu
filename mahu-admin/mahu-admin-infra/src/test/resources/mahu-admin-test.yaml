info:
  name: mahu-admin
  version: SNAPSHOT
  env: sit

db:
  application-name: "mahu-integration-test"
  driver: "org.postgresql.Driver"
  url: "${MAHU_DB_URL}"
  username: "${MAHU_DB_USERNAME}"
  password: "${MAHU_DB_PASSWORD}"
  min-idle: 1
  max-size: 64
