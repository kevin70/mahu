apply plugin: "java-library"
apply plugin: "io.ebean"

dependencies {
    annotationProcessor(libs.ebean.querybean.generator)
    annotationProcessor(libs.helidon.bundles.apt)

    api(project(":mahu-entity"))
    api(project(":mahu-config"))
    api(project(":mahu-shared"))
    api(libs.bundles.jackson)
    api(libs.bundles.helidon.service)
    implementation(libs.bundles.helidon.webclient)
    implementation(libs.password4j)
    implementation(libs.minio)
    implementation(libs.caffeine)
    implementation(libs.roaringbitmap)
    implementation("com.rabbitmq.client:amqp-client:0.8.0")

    implementation(libs.bundles.ebean)
    implementation(libs.bundles.log4j)
    //    implementation("com.github.binarywang:weixin-java-miniapp")
    //    implementation("com.github.binarywang:weixin-java-pay")
    // implementation("io.valkey:valkey-glide:2.2.5:${osdetector.classifier}")
    implementation(libs.valkey.glide) {
        artifact {
            classifier = osdetector.classifier
        }
    }
    implementation("org.apache.rocketmq:rocketmq-client-java-noshade:5.1.0")
    implementation("org.apache.pulsar:pulsar-client-original:4.1.2")

    // testImplementation(project(":mahu-test"))
    //    testImplementation("io.helidon.testing:helidon-testing-junit5")
}
