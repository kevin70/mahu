apply plugin: "java"
apply plugin: "checkstyle"

sonarLint {
    languages {
        include("java")
    }
    ignoredPaths = [
        "cool/houge/mahu/Base58.java"
    ]
    testSourceSets = sourceSets.matching { true }
}
